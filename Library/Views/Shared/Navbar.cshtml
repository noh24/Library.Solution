@* <div class="d-flex">
  <p class="mr-2">@Html.ActionLink("Authors", "Index", "Authors")</p>
  <p class="mr-2">@Html.ActionLink("Books", "Index", "Books")</p>
  <p class="mr-2">@Html.ActionLink("Copies", "Index", "Copies")</p>
  <p class="mr-2">@Html.ActionLink("CheckOuts", "Index", "CheckOuts")</p>
  <p class="mr-2">@Html.ActionLink("Create or manage an account", "Index", "Account")</p>
  </div> *@
<div class="nav flex-row nav-pills" id="v-pills-tab" role="tablist" aria-orientation="horizontal">
  <a class="nav-link active" data-toggle="pill" role="tab" href="/">Home</a>
  <a class="nav-link active" data-toggle="pill" role="tab" href="/authors">Authors</a>
  <a class="nav-link active" data-toggle="pill" role="tab" href="/books">Books</a>
  <a class="nav-link active" data-toggle="pill" role="tab" href="/copies">Copies Available</a>
  <a class="nav-link active" data-toggle="pill" role="tab" href="/checkouts">Checkouts</a>
  @if (User.Identity.IsAuthenticated)
  {
    <a class="nav-link active" data-toggle="pill" role="tab" href="/account/">Account</a>
    @using (Html.BeginForm("LogOff", "Account"))
    {
      <input type="submit" class="nav-link nav-logout" value="Logout" />
    }
  }
  else
  {
    <a class="nav-link active" data-toggle="pill" role="tab" href="/account/login">Login</a>
    <a class="nav-link active" data-toggle="pill" role="tab" href="/account/register">Register</a>
  }


</div>